version: '3'
services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    depends_on:
      - db
    environment:
      - DATABASE_URL=postgres://myuser:mypostgrespassword@db/test
  db:
    image: postgres:14.1
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=open-flights
      - POSTGRES_PASSWORD=mypostgrespassword
      - POSTGRES_DB=test
